name: NPM Publish

# Automatically publishes to npm on push to main branch

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.github/workflows/docs.yml'
      - 'examples/**'
    # Skip if commit message contains [skip ci] or version bump
    # This prevents infinite loops from automated version bumps

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    # Skip if this is a version bump commit to prevent infinite loop
    if: "!contains(github.event.head_commit.message, '[skip ci]') && !contains(github.event.head_commit.message, 'chore: bump version')"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Check current version
        id: version
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          echo "current=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version in package.json: $CURRENT_VERSION"

      - name: Get published version from npm
        id: npm-version
        run: |
          NPM_VERSION=$(npm view @nodelibraries/ioc version 2>/dev/null || echo "0.0.0")
          echo "published=$NPM_VERSION" >> $GITHUB_OUTPUT
          echo "Published version on npm: $NPM_VERSION"

      - name: Bump version if needed
        id: bump
        run: |
          CURRENT="${{ steps.version.outputs.current }}"
          PUBLISHED="${{ steps.npm-version.outputs.published }}"

          if [ "$CURRENT" == "$PUBLISHED" ]; then
            echo "Version unchanged, bumping patch version..."
            npm version patch --no-git-tag-version
            NEW_VERSION=$(node -p "require('./package.json').version")
            echo "bumped=true" >> $GITHUB_OUTPUT
            echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
            echo "Bumped to: $NEW_VERSION"
          else
            echo "Version already changed ($PUBLISHED -> $CURRENT), using current version"
            echo "bumped=false" >> $GITHUB_OUTPUT
            echo "new_version=$CURRENT" >> $GITHUB_OUTPUT
          fi

      - name: Commit version bump
        if: steps.bump.outputs.bumped == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "chore: bump version to ${{ steps.bump.outputs.new_version }} [skip ci]"
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main

      - name: Verify npm authentication
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "Verifying npm authentication..."
          NPM_USER=$(npm whoami --registry=https://registry.npmjs.org 2>&1)
          if [ $? -ne 0 ]; then
            echo "❌ npm authentication failed. Please check NPM_TOKEN secret."
            echo "Error: $NPM_USER"
            exit 1
          fi
          echo "✅ npm authentication successful"
          echo "Logged in as: $NPM_USER"

      - name: Check package existence and scope access
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "Checking if package exists on npm..."
          if npm view @nodelibraries/ioc version 2>/dev/null; then
            echo "✅ Package exists on npm"
            EXISTING_VERSION=$(npm view @nodelibraries/ioc version)
            echo "Current published version: $EXISTING_VERSION"
          else
            echo "⚠️  Package does not exist yet. This will be the first publish."
            echo ""
            echo "Checking scope access..."
            SCOPE_CHECK=$(npm access ls-packages @nodelibraries 2>&1)
            if [ $? -eq 0 ]; then
              echo "✅ Can access @nodelibraries scope"
              echo "$SCOPE_CHECK"
            else
              echo "❌ Cannot access @nodelibraries scope"
              echo "Error: $SCOPE_CHECK"
              echo ""
              echo "Possible solutions:"
              echo "1. Create @nodelibraries organization on npm.com"
              echo "2. Add your npm user to the @nodelibraries organization"
              echo "3. Ensure NPM_TOKEN is associated with a user who has access to @nodelibraries"
            fi
          fi

      - name: Configure npm for scoped package
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo "Configuring npm for scoped package..."
          echo "@nodelibraries:registry=https://registry.npmjs.org/" >> ~/.npmrc
          echo "//registry.npmjs.org/:_authToken=$NODE_AUTH_TOKEN" >> ~/.npmrc
          echo "✅ npm configured for @nodelibraries scope"

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          VERSION="${{ steps.bump.outputs.new_version }}"
          echo "Publishing version: $VERSION"
          echo "Package: @nodelibraries/ioc"
          echo "Registry: https://registry.npmjs.org"

          # Try to publish with detailed error output
          PUBLISH_OUTPUT=$(npm publish --access public --registry=https://registry.npmjs.org 2>&1)
          PUBLISH_EXIT=$?

          if [ $PUBLISH_EXIT -eq 0 ]; then
            echo "✅ Successfully published @nodelibraries/ioc@$VERSION"
            echo "$PUBLISH_OUTPUT"
          else
            echo "❌ Failed to publish package"
            echo "$PUBLISH_OUTPUT"
            echo ""
            echo "Troubleshooting steps:"
            echo "1. Verify NPM_TOKEN is an 'Automation' token with publish permissions"
            echo "2. Ensure the token's user has access to @nodelibraries organization on npm"
            echo "3. Check if you're a member of @nodelibraries organization: npm team ls @nodelibraries"
            echo "4. Verify token permissions: npm token list"
            echo "5. Try publishing manually: npm publish --access public"
            exit $PUBLISH_EXIT
          fi
